# 前端快速启动指南

## 快速开始

### 1. 安装依赖

```bash
cd frontend
npm install
```

### 2. 配置环境变量（可选）

创建 `.env` 文件（如果后端不在默认端口）：

```bash
VITE_API_URL=http://localhost:8080/api/v1
```

### 3. 启动开发服务器

```bash
npm run dev
```

前端将在 `http://localhost:3000` 启动。

### 4. 访问应用

打开浏览器访问：`http://localhost:3000`

## 默认账户

使用测试脚本创建的账户：
- 邮箱: `test@example.com`
- 密码: `Test1234!`

或注册新账户。

## 功能说明

### 登录/注册
- 支持用户注册和登录
- 自动保存Token，刷新页面保持登录状态

### 仪表盘
- 显示翻译统计信息
- 快速操作入口

### 翻译功能
- 支持8种语言互译
- 实时翻译
- 复制翻译结果

### 翻译历史
- 查看所有翻译记录
- 搜索功能
- 分页显示

### 用户管理
- 查看当前用户信息
- 账户状态

## 故障排查

### 无法连接后端

1. 确保后端服务运行：
   ```bash
   docker-compose ps
   ```

2. 检查后端健康状态：
   ```bash
   curl http://localhost:8080/health
   ```

3. 检查 `.env` 文件中的API URL

### 样式不显示

1. 确保安装了所有依赖：`npm install`
2. 重启开发服务器

### 登录后跳转失败

检查浏览器控制台错误，可能是Token验证失败。

## 下一步

- 查看完整文档：`README.md`
- 自定义样式：修改 `tailwind.config.js`
- 添加新功能：参考现有页面组件

